#ifndef __HARMONIC_SHIFT_OSCILLATOR_HPP__
#define __HARMONIC_SHIFT_OSCILLATOR_HPP__

/**
 * Re = sum(pow(L, n) * sin((1 + n * s) * phi * t)) / (2 * |L| + 1)
 * Im = sum(pow(L, n) * cos((1 + n * s) * phi * t)) / (2 * |L| + 1)
 * L = [-0.5 : 0.5]
 * s = [-4 : 4]
 * 
 * 8 harmonics seems sufficient
 */

#endif
